export const environment = {
    production: false,          // Set to true for production environment
    apiUrl: 'http://localhost:3000', 
    // // The backend API URL (adjust if needed)
    jwtSecretKey: 'secretKey',
  };
  